import { RequiredSignDirective } from './required-sign.directive';

describe('RequiredSignDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredSignDirective();
    expect(directive).toBeTruthy();
  });
});
